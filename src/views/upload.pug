extends base.pug

block content 
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(required, placeholder="Title", type="text", name="title", maxlength=80)
        input(required, placeholder="Description", type="text", name="description", minlength=20)
        input(required, placeholder="Hashtags, separated by comma.", type="text", name="hashtags")
        input(type="submit", value="Upload Video")